/*global $, console */
var APP = (function () {
    'use strict';

    //private methods
    function init() {
        
    }

    //public methods
    return {
        Init: function () {
            init();
        }
    };
}());

$(document).ready(function () {
    'use strict';
    APP.Init();
});